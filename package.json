{
  "name": "srt",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://sunriseracing.team/",
  "dependencies": {
    "axios": "^0.21.1",
    "cheerio": "^1.0.0-rc.5",
    "handlebars": "^4.7.6"
  },
  "scripts": {
    "clean": "rm -f data/riders.json data/signups_1793* data/teams.json",
    "build": "npm run clean && node bin/buildTeams.js",
    "deploy": "gh-pages -d site -f"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
